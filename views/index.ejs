<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <!--conteudo da tag head-->
    <%- include ('partials/head.ejs'); %>
</head>

<body class="bg-color">
    <!--navbar-->
    <%- include('partials/nav.ejs'); %>
    <!--Tilo-->
    <div class="container">
        <hr>
        <h1 class="title title-gold">Faça Sua Pergunta</h1>
            <a href="/perguntar" class="btn btn-warning"> Perguntar</a>
        <hr>
        </div>
        <div class="container">

            <% perguntas.forEach(pergunta=>{ %>
                <!--Cria um card-->
                </br>
                <div class="card">
                    <!--Titulo da pergunta-->
                    <div class="card-body">
                        <h3>
                            <%= pergunta.titulo %>
                        </h3>
                    </div>
                    <!--Botao-->
                    <div class="card-footer">
                        <button class="btn btn-warning">Responder</button>
                    </div>
                </div>
                <% }) %>
        </div>
        <!--footer-->
        <%- include('partials/footer.ejs'); %>
</body>

</html>